
This section focuses on \textbf{ExoType} and \textbf{ExoTarget} predictors, which classify bacterial exotoxin types and targets, respectively. In this thesis, \textbf{six different predictors} are developed—three for ExoType and three for ExoTarget—to evaluate their performance under different variations. These three predictors vary in \textbf{input types (protein sequences and protein folds)} and \textbf{data splitting approaches (standard split and redundancy reduction based split)}.

The results section begins with data exploration and preprocessing, outlining key dataset characteristics and preparation steps. Afterwards, feature analysis and model performance is shown.

First, Figure~\ref{fig:dataset_distribution} examines the distribution of labels in the dataset, including exotoxin type and target.

\begin{figure}[H]
	\centering\includegraphics[width=0.5\textwidth]{images/upset_plot_of_raw_data.png}
	\caption{\small Upset plot showing the distribution of exotoxin types and their host targets. The upper bar chart represents intersection sizes, while the lower matrix maps each exotoxin type (Type I, II, III, IV, Unknown) and host target (Vertebrate, Invertebrate) to their intersections. Horizontal bars on the left indicate total occurrences per category.}
	\label{fig:dataset_distribution}
\end{figure}

The dataset shows a strong imbalance, with Type III exotoxins being the most prevalent at 2,149 sequences, while the 'Unknown' category is underrepresented with only 104 samples. Vertebrate-targeting exotoxins are more common, with 1,863 sequences compared to 629 for invertebrate-targeting exotoxins.

\section{Data Exploration and Processing}
Following the analysis of data distribution, the next step examines \textbf{key sequence properties such as sequence length and hydrophobicity} to identify patterns and potential biases in the dataset. For sequence length analysis, the box plots in Figure~\ref{fig:sequence_length_by_type} display the distribution of sequence lengths for each exotoxin type, while Figure~\ref{fig:sequence_length_by_target} presents the distribution based on exotoxin targets.

\begin{figure}[H]
	\centering
	\begin{subfigure}[b]{0.6\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/sequence_length_by_type.png} 
		\caption{Sequence length distribution by exotoxin type.}
		\label{fig:sequence_length_by_type}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.6\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/sequence_length_by_target.png}
		\caption{Sequence length distribution by exotoxin target.}
		\label{fig:sequence_length_by_target}
	\end{subfigure}
	\caption{\small Sequence length distribution of bacterial exotoxins. (a) Distribution across different exotoxin types as boxplots, measured in amino acids. Exotoxin types are color-coded: Type I (blue), Type II (orange), Type III (pink), Type IV (green), and Unknown (yellow). (b) Distribution across exotoxin targets, categorized as vertebrates (dark green) and non-vertebrates (purple).}
	\label{fig:sequence_length_distribution}
\end{figure}

Figure \ref{fig:sequence_length_by_type} shows that average sequence lengths differ by toxin type. Type I toxins have a mean length of 239.9 amino acids. Type II toxins average 329.4 amino acids. Type III toxins measure about 448 amino acids. Type IV toxins average 706 amino acids, and toxins labeled as Unknown average about 607.8 amino acids. In contrast, Figure \ref{fig:sequence_length_by_target} shows that non-vertebrate targets average around 460 amino acids, while vertebrate targets average about 438 amino acids. 

 \gls{GLM} tests the statistical significance of these differences among types and targets. Compared to the overall average, Type I toxins are shorter by about 228 amino acids, Type II toxins are shorter by about 136 amino acids, and Type IV toxins are longer by about 237 amino acids (all p values are below 0.002). 
The  \gls{GLM} shows that toxin type is the primary factor driving sequence length variability. Plus, the model finds no significant difference for Type III toxins, and it indicates a significant effect of the target variable on sequence length.
%These findings suggest that the classification of exotoxins based on sequence length is largely driven by the toxin type rather than the biological target.

Figure~\ref{fig:hydrophobicity_toxin_type} and Figure~\ref{fig:hydrophobicity_by_target} display hydrophobicity per residue per residue across different toxin types and targets to determine whether there is a feature pattern among groups. 

%mean values by 'type':
%Hydrophobicity  Sequence_Length
%type                                     
%Type_I         -0.399536       239.869565
%Type_II        -0.192125       329.469027
%Type_III       -0.358247       448.009302
%Type_IV        -0.440321       706.000000
%Unknown        -0.300758       607.857143

%Mean values by 'target':
%Hydrophobicity  Sequence_Length
%target                                         
%Non-vertebrate       -0.316875       460.656958
%Vertebrate           -0.345380       438.744681

\begin{figure}[H]
	\centering
	\begin{subfigure}[b]{0.6\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/hydrophobicity_by_type.png} 
		\caption{Hydrophobicity distribution by exotoxin type.}
		\label{fig:hydrophobicity_toxin_type}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.6\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/hydrophobicity_by_target.png} 
		\caption{Hydrophobicity distribution by exotoxin target.}
		\label{fig:hydrophobicity_by_target}
	\end{subfigure}
	\caption{\small Hydrophobicity distribution of bacterial exotoxin types and targets. (a) The x-axis represents exotoxin types, color-coded: Type I (blue), Type II (orange), Type III (pink), Type IV (green), and Unknown (yellow). Box plots show the median, interquartile range, whiskers extending to 1.5 times the interquartile range, with outliers as separate points. (b) The x-axis represents exotoxin targets: Vertebrates (dark green) and Non-vertebrates (purple).}
	\label{fig:hydrophobicity_distribution}
\end{figure}

Figure \ref{fig:sequence_length_by_type} shows that average sequence lengths differ by toxin type. Type I toxins have a mean length of 239.9 amino acids. Type II toxins average 329.4 amino acids. Type III toxins measure about 448 amino acids. Type IV toxins average 706 amino acids, and toxins labeled as Unknown average about 607.8 amino acids. In contrast, Figure \ref{fig:sequence_length_by_target} shows that non-vertebrate targets average around 460 amino acids, while vertebrate targets average about 438 amino acids. 

Figure~\ref{fig:hydrophobicity_toxin_type}, shows hydrophobicity differ by toxin type. Type I toxins have a hydrophobicity of -0.40. Type II toxins average -0.19 amino acids. Type III toxins measure about -0.36. Type IV has a hydrophobicity value of -0.44 and toxins labeled as Unknown has hydrophobicity  of 0.30. In contrast, it shows that non-vertebrate targets hydrophobicity value of  0.31, while vertebrate targets has a value of 0.34.  Figure~\ref{fig:hydrophobicity_by_target} shows that vertebrate- and invertebrate-targeting exotoxins exhibit largely overlapping hydrophobicity distributions, with vertebrate-targeting toxins displaying slightly higher variability. However, the overlap suggests that hydrophobicity alone does not strongly differentiate exotoxin targets.

 \gls{GLM} analysis confirms that only Type II toxins show a statistically significant increase in hydrophobicity with a coefficient of 0.1461 and p value lower than 0.001. In a nutshell, no significant differences were found between target groups or other toxin types. These findings suggest that hydrophobicity may help distinguish certain toxin types, particularly Type II, but it is not a strong predictor for toxin targets. 


\subsection{Redundancy Reduction with Protein Sequences}

Table~\ref{tab:merged_dataset_distribution}, displays results of redundancy reduction with 30\% similarity threshold. The Table~\ref{tab:exotoxin_types_merged} contains 42 Type I, 169 Type II, 2149 Type III, 28 Type IV, and 104 Unknown sequences. 
After redundancy reduction, the counts drop to 29, 145, 852, 25, and 93 respectively. This method reduces the overall sequence count from 2,492 to 1,068. Notably, the percentage of Type III drops from 86\% to 74\% . On the other hand, in Table~\ref{tab:exotoxin_targets_merged} original dataset contains 1863 Vertebrates and 614 Non-Vertebrates. This reduction decreases sequence similarity within the dataset, increasing the unique characteristics and functional diversity of exotoxins before training.

\begin{table}[H]
	\centering
	\caption{Summary of the dataset distribution based on exotoxin types and their biological targets before and after redundancy reduction using MMseqs2.}
	
	\label{tab:merged_dataset_distribution}
	
	\begin{subtable}[t]{0.45\textwidth}
		\centering
		\caption{}
		\label{tab:exotoxin_types_merged}
		\begin{tabular}{@{}lll@{}}
			\toprule
			\textbf{Exotoxin Type} & \textbf{Original} & \textbf{Reduced} \\ \midrule
			Type I & 42 & 29 \\
			Type II & 169 & 145 \\
			Type III & 2149 & 852 \\
			Type IV & 28 & 25 \\
			Unknown & 104 & 93 \\
			\bottomrule
		\end{tabular}
	\end{subtable}
	\hfill
	\begin{subtable}[t]{0.45\textwidth}
		\centering
		\caption{}
		\label{tab:exotoxin_targets_merged}
		\begin{tabular}{@{}lll@{}}
			\toprule
			\textbf{Exotoxin Target} & \textbf{Original} & \textbf{Reduced} \\ \midrule
			Vertebrates & 1863 & 614 \\
			Invertebrates & 629 & 234 \\
			\bottomrule
		\end{tabular}
	\end{subtable}
\end{table}



\section{Data Transformation}

This project uses two different raw data: \textbf{protein folds} or \textbf{protein sequences}. 

In the next stages, machine learning pipeline uses 1067 nonredundant protein sequences as raw data. As a second raw data, it uses protein sequence information to predict 3D structures information that can be referred as protein folds. This analysis looks into the quality of predicted protein folds by AlphaFold. Notably, both sequence-based and fold-based embeddings have 1024 dimensions.

Figure~\ref{fig:lddt_score} displays the distribution of \gls{pLDDT} scores for nonredundant protein folds. It to evaluates the confidence of our protein structure predictions; higher scores indicate greater model confidence, while lower scores indicate lower model confidence.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\textwidth]{images/LDDT.png}
	\caption{\small \textbf{\Gls{pLDDT}} score to assess fold quality. The black line is mean  \gls{pLDDT} score across samples. The gray-colored region stands for one standard deviation around the mean, capturing the spread of  \gls{pLDDT} scores within each sample. The red data points indicate the median \gls{pLDDT} scores, providing an alternative measure of central tendency and highlighting any deviations from the mean.}
	\label{fig:lddt_score}
\end{figure}

Approximately 68\% of the data falls within the grey region, corresponding to one standard deviation around the mean. The distribution shows that most  \gls{pLDDT} scores lie between 40 and 100, with increasing variability as the mean score rises.  \gls{pLDDT} values below 50 are present in a subset of samples, but the majority of structures show mean  \gls{pLDDT} values above 60, suggesting overall reasonable confidence in the predictions. This analysis is useful in assessing the quality of predicted 3D structures before the prediction. 

Then, the protein embedding structures indicate that both sequence-based and fold-based embeddings have 1024 dimensions.

\subsection{Dimensionality Reduction with Protein Embeddings}

\gls{PCA} is used to check clustering patterns of bacterial exotoxins and their targets using both regular and scrambled protein embeddings. The results reveal important insights into the role of sequence-based embeddings in toxin classification.

The \gls{PCA} of non-scrambled sequence based embeddings (Figure~\ref{fig:pca_toxin_type}) shows clustering among toxin types. Principal component 1 is 9.72 \% and principal component 2 is 5.54\%.

The \gls{PCA} of scrambled embeddings (Figure~\ref{fig:scrambled_pca_toxin_type}) reveals no meaningful structure, with all toxin types overlapping substantially. This lack of clustering indicates that the scrambled embeddings fail to capture meaningful toxin-specific information, as opposed to the regular embeddings.

	\begin{figure}[H]
		\centering
		\begin{subfigure}[b]{0.48\textwidth}
			\centering
			\includegraphics[width=\textwidth]{images/pca_projection_type_nonscrambled.png} 
			\caption{}
			\label{fig:pca_toxin_type}
		\end{subfigure}
		\hfill
		\begin{subfigure}[b]{0.48\textwidth}
			\centering
			\includegraphics[width=\textwidth]{images/pca_projection_type_scrambled.png} 
			\caption{}
			\label{fig:scrambled_pca_toxin_type}
		\end{subfigure}
		\caption{\small \gls{PCA} projections comparing original and scrambled protein sequences for bacterial exotoxin types. The subfigure (a) belongs to non-scrambled, while the subfigure (b) belongs to scrambled sequence based embeddings. The x-axis represents Principal Component 1, and the y-axis represents Principal Component 2. Each exotoxin type is color-coded: Type I (blue), Type II (purple), Type III (pink), Type IV (green), and Unknown (yellow).}
		\label{fig:dimensionality_reduction_toxin_type}
	\end{figure}

Similarly, based on non-scrambled sequence-based embeddings, Figure~\ref{fig:pca_projection_non_scrambled_target} shows small clustering patterns among toxin targets. Principal component 1 is 9.72 \% and principal component 2 is 5.54\%.
Based on scrambled sequence-based embeddings, Figure~\ref{fig:pca_projection_scrambled_target} reveals no meaningful structure. This confirms vertebrate and invertebrate sequences exhibit biologically meaningful clustering in non-scrambled ones.


\begin{figure}[H]
	\centering
	\begin{subfigure}[b]{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/pca_projection_target_nonscrambled.png} 
		\caption{}
		\label{fig:pca_projection_non_scrambled_target}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/pca_projection_target_scrambledd.png}
		\caption{}
		\label{fig:pca_projection_scrambled_target}
	\end{subfigure}
	\caption{\small \gls{PCA} projections comparing original and scrambled protein sequences for bacterial exotoxin targets. The subfigure (a) belongs to non-scrambled, while the subfigure (b) belongs to scrambled sequence based embeddings. The x-axis represents Principal Component 1, and the y-axis represents Principal Component 2. Targets are color-coded: Vertebrates (purple) and Non-vertebrates (dark green). Scrambling reduces cluster compactness, demonstrating the loss of structure in the data.}
	\label{fig:dimensionality_reduction_toxin_target}
\end{figure}

\section{ExoType Predictor}
This section describes the development of the ExoType predictor, which classifies exotoxins into five categories: \textbf{Type I, Type II, Type III, Type IV, and Unknown}. The following subsections outline the training and evaluation of the model using two alternative data representations:

\begin{itemize}
	\item Sequence-based embeddings,
	\item Fold-based embeddings 
\end{itemize}

\subsection{ExoType Dataset}
This section proposes two data-splitting strategies and the class distribution among exotoxin types: Type I, Type II, Type III, Type IV, Unknown. Two data-splitting strategies are used in the Section~\ref{section:training_test_set}. Table~\ref{tab:data_distribution_type} shows the class distribution after data splitting. 

In the standard split, the training set contains 10 Type I, 77 Type II, 699 Type III, 14 Type IV, and 48 Unknown sequences whereas in the redundancy split, the sequence counts increases to 12 Type I, 96 Type II, 875 Type III, 18 Type IV, and 60 Unknown sequences. Additionally, the number of test samples in redundancy reduction split increases 2 to 25 in Type I, 19 to 68 Type II, 176 to 1178 Type III, 4 to 9 Type IV, and 12 to 39 Unknown sequences.

\begin{table}[h]
	\centering
	\caption{Class distribution before and after redundancy reduction using two different splitting approaches.}
	\resizebox{\textwidth}{!}{ 
		\begin{tabular}{lcccc}
			\toprule
			\textbf{Class} & \shortstack{\textbf{Standard Split:} \\ \textbf{Training Data}} 
			& \shortstack{\textbf{Redundancy Reduction Split:} \\ \textbf{Training Data}} 
			& \shortstack{\textbf{Standard Split:} \\ \textbf{Test Data}} 
			& \shortstack{\textbf{Redundancy Reduction Split:} \\ \textbf{Test Data}} \\
			\midrule
			Type I     & 10  & 12  & 2  & 25  \\
			Type II    & 77  & 96  & 19 & 68 \\
			Type III   & 699 & 875 & 176 & 1178 \\
			Type IV    & 14  & 18  & 4  & 9  \\
			Unknown    & 48  & 60  & 12 & 39 \\
			\midrule
			\bottomrule
	\end{tabular}}
	\label{tab:data_distribution_type}
\end{table}

Data splitting methods used in Table~\ref{tab:data_distribution_type} (standard split and redundancy reduction split) explained in Section~\ref{section:training_test_set}.


\subsection{Training Performance}

Four models with different architectures were trained to predict different exotoxin types: \textbf{Logistic Regression, Support Vector Machines, K-Nearest Neighbours, and Random Forest}.

%Below the training performance of both embedding types are discussed with the help of the cross validation mcc and the learning curves. Table~\ref{tab:mcc_comparison} shows the MCC cross validation values of different architectures using sequence based embeddigns while Table~\ref{tab:standard_split_mcc} shows the the MCC cross validation scores of the fold based embeddings. 

\subsubsection{Sequence-based Embeddings:}  

\begin{table}[h]
	\centering
	\caption{Comparison of cross-validation \gls{MCC} scores for different models using Standard Split and Redundancy Reduction Split based on fold based embeddings for ExoType. The \gls{MCC}  is a metric that takes place between –1 and +1. The value of +1 means the predictions perfectly match the true classes. 0 means the model is guessing randomly.}
	\resizebox{\textwidth}{!}{ 
		\begin{tabular}{lcc}
			\toprule
			\textbf{Model} & \textbf{Standard Split MCC} & \textbf{Redundancy Reduction Split MCC} \\
			\midrule
			Random Forest           & 0.62 ± 0.07 & 0.62 ± 0.01 \\
			Logistic Regression     & 0.62 ± 0.05 & 0.64 ± 0.03 \\
			Support Vector Machine  & 0.63 ± 0.04 & 0.65 ± 0.04 \\
			K-Nearest Neighbors     & 0.74 ± 0.06 & 0.73 ± 0.02 \\
			\bottomrule
		\end{tabular}
	}
	\label{tab:mcc_comparison_type}
\end{table}

Table~\ref{tab:mcc_comparison_type} depicts cross-validation \gls{MCC} scores with their error values among models with different machine learning algorithm with two data-splitting strategies. The cross-validation \gls{MCC} scores indicates that data splitting methods doesn't significantly make a difference in training performance as it is visible in Random Forest (0.62 ± 0.07 to 0.62 ± 0.01), Logistic Regression (0.62 ± 0.05 to 0.64 ± 0.03), \gls{SVM} (0.63 ± 0.04 to 0.65 ± 0.04), K-Nearest Neighbors (KNN) (0.74 ± 0.06  to 0.73 ± 0.02). 

\begin{figure}[H]
	\centering
	\includegraphics[width=0.99\textwidth]{images/learning_curve_type_split.png}
	\caption{\small Learning Curve for Protein Sequence-Based Embeddings: This graph is used to assess model generalization when the dataset is split into 80\% training and 20\% test sets.}
	\label{fig:split_test_learning_curves}
\end{figure}

Figure~\ref{fig:split_test_learning_curves} displays the \gls{MCC} cross validation changes across training and validation sets. The analysis of learning curvecan be assessed in two phases: the process of the model learning it with training \gls{MCC} curve and the level of generalization the model reaches. The training \gls{MCC} of Random Forest, the training \gls{MCC} conclude 0.62 at 600 samples and it converges with validation curve which is a sign of improvement in generalization. In contrast, the training \gls{MCC} remains at 1.00 during 700 training examples in Random Forest and \gls{KNN} models.

In Appendix, Figure~\ref{fig:learning_curve_type_import} uses the redundancy split training set as input. In learning curves, it is visible that as the training sample size increases, the overfitting trend decreases among models e.g. \gls{KNN}. Additionally, for both the Logistic Regression model and \gls{SVM}, the training \gls{MCC}and validation \gls{MCC} values converge at 480 training examples, suggesting improved generalization and model performance.


\subsubsection{Fold-based Embeddings:}  

\begin{table}[h]
	\centering
	\caption{Cross-validation \gls{MCC} scores for different models using the sequence-based embeddings for ExoType.}
	\begin{tabular}{lc}
		\toprule
		\textbf{Model} & \textbf{Standard Split MCC} \\
		\midrule
		Random Forest           & 0.16 ± 0.11 \\
		Logistic Regression     & 0.36 ± 0.03 \\
		Support Vector Machine  & 0.37 ± 0.03 \\
		K-Nearest Neighbors     & 0.38 ± 0.03 \\
		\bottomrule
	\end{tabular}
	\label{tab:standard_split_mcc}
\end{table}

In Table~\ref{tab:standard_split_mcc} in terms of cross-validation \gls{MCC} based on sequence embeddings, the original approach achieved values of 0.62 ± 0.11, 0.62 ± 0.03, 0.63 ± 0.03, and 0.74 ± 0.03 for Random Forest, Logistic Regression, \gls{SVM}, and \gls{KNN} respectively. With the redundancy reduction split, Random Forest had an \gls{MCC} of 0.62 ± 0.11, while Logistic Regression and \gls{SVM} improved to 0.64 ± 0.04 and 0.65, respectively, and \gls{KNN} decreased to 0.73 ± 0.03. These changes indicate that while the redundancy reduction split has improved the performance of some models, due to the increasing of the minority classes, the overall impact is similar. 

%The class imbalance in the second approach may limit the models' ability to generalize better, indicating that while there is a hint of improvement, additional strategies to further address the imbalance might be necessary to prevent the potential underfitting of the minority classes.
%In addition to the numerical comparisons, the learning curves provide further insight into how each model responds to increasing amounts of training data under both approaches. In Figure~\ref{fig:split_test_learning_curves}, the learning curves for these four models display the training and validation MCC scores across various training set sizes. Ideally, as the amount of training data increases, the training MCC should decline slightly—indicating that the model stops overfitting and starts to generalize better to unseen data.  k
%For Random Forest and KNN, the training MCC remains nearly constant, suggesting these models continue to overfit the training data. Conversely, SVM and Logistic Regression exhibit lower training MCC scores, implying they do not overfit. Moreover, although the validation MCC scores for all models improve with more data, SVM and Logistic Regression appear more balanced between fitting the training data and generalizing well. This pattern indicates that SVM and Logistic Regression are more suitable for the classification of toxin types using sequence-based embeddings. 


\begin{figure}[H]
	\centering
	\includegraphics[width=0.99\textwidth]{images/learning_curves_type_folds.png}
	\caption{\small Learning Curve for Protein Fold-Based Embeddings: This graph is used to assess model generalization when the dataset is split into 80\% training and 20\% test sets.}
	\label{fig:learningcurve_fold_type}
\end{figure}

In Figure~\ref{fig:learningcurve_fold_type}, the fold-based learning curves for the embeddings based on protein folds illustrate how each classifier's \gls{MCC} on the training and validation sets evolves as more training samples become available. Random Forest shows a relatively stable performance but remains at a low validation \gls{MCC} of 0.16 in cross-validation, suggesting that while it fits the training data quickly and it struggles to generalize. Logistic Regression (0.36), \gls{SVM} (0.37), and \gls{KNN} (0.38) exhibit modest improvements in validation \gls{MCC} with increasing training size, but none converge on particularly high \gls{MCC} values. 

%These trends underscore the challenge posed by class imbalance and potential redundancy in the data, as well as the need for more extensive feature engineering or alternative approaches to further boost model generalization.

\subsection{Model Performance Evaluation}

This section evaluates the performance of five machine learning models using the  \gls{MCC}: Random Forest , Logistic Regression, Support Vector Machine, K-Nearest Neighbors, Hierarchical Random Forest model, Hierarchical Support Vector Machine model, and \gls{BLAST} predictor. \gls{MCC} bar plots, precision-recall curves, and confusion matrices assess each model’s generalization to test data. \gls{BLAST}, a non-machine-learning, sequence-based predictor operating on sequence similarity, serves as a comparison to embedding-based machine learning models.

\subsubsection{MCC Bar Plots}
\begin{figure}[H]
	\centering
	\begin{subfigure}{0.49\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/mcc_type_split.png}
		\caption{}
		\label{fig:mcc_sequence_type}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.49\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/mcc_type_folds.png}
		\caption{}
		\label{fig:mcc_fold_type}
	\end{subfigure}
	\caption{\small Comparison of \gls{MCC} scores for different classification models. The x-axis represents different classification models: Random Forest (RF, blue), Logistic Regression (LR, orange), Support Vector Machine (SVM, green), K-Nearest Neighbors (KNN, pink), Hierarchical Random Forest (Hier\_RF, yellow), Hierarchical Support Vector Machine (Hier\_SVM, light blue), and \gls{BLAST}  predictor (brown). The y-axis indicates the  \gls{MCC} scores. (a) The first \gls{MCC} Plot corresponds to the first approach, where the dataset was split into 80\% training and 20\% test sets. Error bars represent the standard deviation across cross-validation folds. (b) Fold-based embeddings}
	\label{fig:mcc_comparison}
\end{figure}

 %In light of the learning curves however it can be assumed that Random Forest and KNN overfitted while SVM and logistic regression generalized better. Overall the errorbars of all models overlap, the differences between the model performances are therefore not significant.  This indicates that using sequence based embeddings to predic the toxin types is not an improvment over using BLAST to match toxins to their closest toxin types. 
%For this approach BLAST significantly outperformed the fold embeddings. 
%This suggests that the fold based approach is less effective for exotoxin types prediction compared to sequence based embedings approach or the existing BLAST. However, fold-based embeddings still provide valuable structural information.

Figure~\ref{fig:mcc_sequence_type} displays the final \gls{MCC} values for \textbf{sequence-based embeddings} using a \textit{standard split}.
Random Forest has a \gls{MCC} score of 0.50±0.15 and \gls{SVM} achieves an \gls{MCC} of 0.62±0.11, Logistic Regression reaches 0.58±0.11, and \gls{KNN} scores about 0.65±0.13 which suggests \gls{KNN} has a better performance than other models. In terms of SVM  and Random Forest Hierarchical predictors, their scores consist of 0.62±0.11 and 0.45±0.14. Apart from the embedding-based models, a sequence-based model has a \gls{MCC} score of 0.67±0.09. Finally, as the test data size increases under a \textit{redundancy reduction split} (see Figure~\ref{fig:MCC_type_import} in the Appendix), Random Forest and \gls{KNN}  improve to an \gls{MCC} around 0.8, while Logistic Regression and SVM maintain a similar \gls{MCC}  as before. The improvement of \gls{KNN}  and RF can indicate increase in data size may benefit model's performance.

Figure~\ref{fig:mcc_fold_target} displays the final \gls{MCC} values for \textbf{fold-based embeddings} using a \textit{standard split}. Random Forest, Logistic Regression, and \gls{SVM} each reach an \gls{MCC} of about 0.6, while \gls{KNN} achieves a lower \gls{MCC} of around 0.5. The decrease of \gls{KNN}'s performance when compared to sequence based embeddings may be linked to the limitation of information gathered from fold based embeddings.


\subsubsection{Precision-Recall Curves}

\begin{figure}[H]
	\centering\includegraphics[width=0.99\textwidth]{images/precisionrecalltypesplit.png}
	\caption{\small Precision-Recall curve for protein sequence based embeddings. In each plot, the x-axis represents Recall, and the y-axis represents Precision. Each plot has the Average Precision (AP) value of Type I, Type II, Type III, Type IV, Unknown . \gls{AP} measures the area under the precision-recall curve. The area under the corresponding curves are a metric for model performance.}
	\label{fig:pr_curve_sequence_type}
\end{figure}

Figure~\ref{fig:pr_curve_sequence_type}  shows the precision-recall curves for Random Forest, Logistic Regression, SVM, KNN - using \textbf{sequence based embeddings} with \textit{standard split}. Each plot has Type I, Type II, Type III, Type IV, Unknown AP values. 

In Random Forest, Type I, II, III, IV, Unknown have a 0.50, 0.70, 0.99, 0.81 and 0.73  \gls{AP} respectively. Logistic Regression, Type I, II, III, IV, Unknown have a  0.26, 0.68, 0.99, 0.62 and 0.58  \gls{AP} respectively. Support Vector Machine, Type I, II, III, IV, Unknown have a  0.55, 0.77, 0.99, 0.48 and 0.46  \gls{AP} respectively. \gls{KNN}, Type I, II, III, IV, Unknown have a  0.50, 0.68, 0.98, 0.89 and 0.74  \gls{AP} respectively.

Overall, Type III is the classified best when compared to other categories. At first, Random Forest and \gls{SVM} shows a better performance for Type I and II across other models. As the test set size increases, Figure~\ref{fig:precision_recall_type_import}, performance of \gls{KNN} improves. Figure~\ref{fig:pr_curve_fold_type}  shows the precision-recall curves for Random Forest, Logistic Regression, \gls{SVM}, \gls{KNN} - using \textbf{fold based embeddings} with \textit{standard split}. Overall, Type I shows a very poor performance across other models. \gls{SVM} and \gls{KNN} outperforms across others.


\subsubsection{Confusion Matrices}
\begin{figure}[H]
	\centering\includegraphics[width=1.2\textwidth]{images/confusionmatrixtypesplit1.png}
	\caption{\small This confusion matrix displays multicass classification protein sequence based embeddings. For each graph, there are sixteen cells that represent the exotoxin type category: Type I, Type II, Type III, Type IV, Unknown. Each cell represents percentage of categories that have a certain true label and were predicted to be a certain class. The diagonal cells shows the correct predictions whereas off-diagonal cells (Type I, II errors) are incorrect predictions. Additionally, the cell shading darkens as the percentage values increase.}
	\label{fig:confusion_matrix_sequence_type}
\end{figure}

In Figure~\ref{fig:confusion_matrix_sequence_type}, \textbf{sequence-based embeddings} using a \textit{standard split}. It is visible that the Random Forest confusion matrix there is a clear dominance of Type III with 99.43\% correct predictions. All Type I and almost all Unknowns are mislabeled as Type III. On the other hand, logistic regression predicts Type III, Type IV correctly by 89\% and 100\% where Type I shows a lower performance by 50\% correct predictions.

Figure~\ref{fig:confusion_matrix_type_import}, \textbf{sequence-based embeddings} using a \textit{redundancy reduction split}, as the test data count increases Random Forest shows a diagonal pattern that indicates an abundancy in correct predictions for all and no dominance of Type III is seen as it was in standard split. Logistic Regression performance also shows and improvement.

Figure~\ref{fig:confusion_matrix_fold_type}, \textbf{fold-based embeddings} using a \textit{standard split}. It is visible that the Random Forest confusion matrix there is a clear dominance of Type III with 100\% correct predictions. All Type I and all Unknowns are mislabeled as Type III. On the other hand, logistic regression predicts shows a relatively poor performance in terms of Type III. Overall poor performance could be related to limited information gathered by protein fold embeddings.



\begin{figure}[H]
	\centering\includegraphics[width=1.2\textwidth]{images/confusionmatrixtypesplit2.png}
	\caption{\small This confusion matrix displays multicass classification protein sequence based embeddings. For each graph, there are sixteen cells that represent the exotoxin type category: Type I, Type II, Type III, Type IV, Unknown. Each cell represents percentage of categories that have a certain true label and were predicted to be a certain class. The diagonal cells shows the correct predictions whereas off-diagonal cells (Type I, II errors) are incorrect predictions. Additionally, the cell shading darkens as the percentage values increase.}
	\label{fig:confusion_matrix_sequence_type_2}
\end{figure}

In Figure~\ref{fig:confusion_matrix_sequence_type_2}, \textbf{sequence-based embeddings} using a \textit{standard split}. It is visible that the \gls{SVM} shows a better performance when compared to Random Forest and Logistic Regression. Confusion matrices display a diagonal prediction pattern that indicates they have an ideal balance between true labels and predicted labels.

Figure~\ref{fig:confusion_matrix_type_import}, \textbf{sequence-based embeddings} using a \textit{redundancy reduction split}, as the test data count increases Random Forest shows a diagonal pattern that indicates an abundancy in correct predictions for all and no dominance of Type III is seen as it was in standard split. Logistic Regression performance also shows and improvement.

Figure~\ref{fig:confusion_matrix_fold_type_2}, \textbf{fold-based embeddings} using a \textit{standard split} with \gls{SVM} and \gls{KNN} models. It is visible that the \gls{KNN} confusion matrix there is a clear dominance of Type III. Majority of Type I and all Unknowns are mislabeled as Type III. On the other hand,  \gls{SVM} does not follow a diagonal patter even though  \gls{SVM} performs best with sequence-based embeddings which could be related to quality of protein fold embeddings.

%Type I is split evenly between correct classifications (50%) and confusion with Type IV. Type II is mostly correct (63.16%) but does get confused with Type III (15.79%) and Unknown (10.53%). Type III again emerges as the best‐recognized class (over 91% correct), while Type IV remains perfectly classified (100%). The Unknown category is correctly identified in the majority (83.33%) of its samples. Overall, SVM strongly differentiates Type III and Type IV, but it still confuses Type I with Type IV, and Type II with both Type III and Unknown.

%Type I is correctly identified half of the time and confused with Type IV the other half (similar to SVM and Logistic Regression). Type III is recognized very confidently (98.30\% correct), though this can come at the cost of mislabeling other classes as Type III, particularly Unknown—half of the true Unknown samples are incorrectly classified as Type III. Meanwhile, Type IV is mostly recognized (75\%) but also gets confused with Type II. Finally, Type II sits at 57.89\% correct but leans more toward being mislabeled as Type III (36.84\%) than as other classes. Overall, KNN does particularly well with Type III, but Type I–Type IV confusion and Unknown–Type III confusion are the main weaknesses.




%Looking at the Random Forest confusion matrix (far left), you can see a strong tendency for the model to lump almost everything into Type III. For example, 100\% of true Type I samples, Type III samples, and Unknown samples are all predicted as Type III. Even Type II and Type IV are mostly misclassified as Type III (over 70\% each). This indicates that Random Forest is failing to differentiate these classes effectively and is disproportionately favoring Type III as its predicted label.

%In the Logistic Regression matrix (top right), there is more variety in predictions, though some significant confusion remains. Notably, none of the true Type I samples are actually classified as Type I; they split evenly between Type IV and Unknown. Type III fares better, with 72.66\% of its samples correctly identified, but it still shows confusion with Type II (7.91\%) and Unknown (12.95\%). Meanwhile, Type IV is correctly identified 75\% of the time, and Type II achieves 50\% accuracy. Although the distribution is less skewed than Random Forest’s, certain classes (especially Type I) remain poorly distinguished.

%The SVM model (bottom left) shows that Type III is recognized with about 78\% accuracy, better than most other classes. Type II lands around 61\% correct, but it tends to be mislabeled as either Type III or Type IV. Interestingly, Type IV is either mistaken for Type II (75\%) or correctly identified (25\%), indicating a substantial mix‐up between these two classes. Unknown is recognized only 25\% of the time, with half of those samples labeled as Type III. Overall, SVM does well for Type III but still struggles separating Type I, II, IV, and Unknown consistently.

%Finally, the KNN confusion matrix (bottom right) also reveals that Type III is the easiest class for the model to learn, with over 95\% correct. Type I samples split evenly between Type II and Type III predictions (50\% each), meaning that KNN never actually labels any Type I samples as Type I. Type II is correct 55.56\% of the time, frequently confused with Type III (44.44\%). Meanwhile, Unknown is most often misinterpreted as Type III (75\%). Thus, while KNN excels in identifying Type III, it struggles notably in correctly recognizing Type I and Unknown.

\pagebreak

\section{ExoTarget Predictor}
This section analyzes ExoTarget predictor that classifies exotoxins into two target based categories: \textbf{Vertebrates} and \textbf{Non-vertebrates}. The following subsections outline the training and evaluation of the model using two different data representations:

\begin{itemize}
	\item Sequence-based embeddings,
	\item Fold-based embeddings 
\end{itemize}



\subsection{ExoTarget Dataset}

This section proposes two data-splitting strategies and the class distribution among exotoxin targets: vertebrates and non-vertebrates. Two data-splitting strategies are used in the Section~\ref{section:training_test_set}. Table~\ref{tab:distribution_target} shows the class distribution after data splitting. 
In the standard split, the training set contains 614 vertebrates and 234 invertebrates, whereas in the redundancy split, the sequence count increases to 768 and 259, respectively. Additionally, the number of test samples in redundancy reduction split (1060) is quite larger than the number of test data of standard split (154). Similarly, the test data of non-vertebrates increases from 59 to 259 after redundancy reduction.

\begin{table}[h]
	\centering
		\caption{Class distribution before and after redundancy reduction using two different splitting approaches. Redundancy Reduction Split and Standard Split}
	\resizebox{\textwidth}{!}{ 
		\begin{tabular}{lcccc}
			\toprule
			\textbf{Class} & \shortstack{\textbf{Standard Split:} \\ \textbf{Training Data}} 
			& \shortstack{\textbf{Redundancy Reduction Split:} \\ \textbf{Training Data}} 
			& \shortstack{\textbf{Standard Split:} \\ \textbf{Test Data}} 
			& \shortstack{\textbf{Redundancy Reduction Split:} \\ \textbf{Test Data}} \\
			\midrule
			Vertebrates     & 614  & 768  & 154  & 1060  \\
			Invertebrates    & 234  & 293  & 59 & 259
			 \\
			\midrule
			\bottomrule
	\end{tabular}}
	\label{tab:distribution_target}
\end{table}


\subsection{Training Performance}
Four machine learning models were trained to predict different exotoxin targets: \textbf{Logistic Regression, Support Vector Machines, K-Nearest Neighbours, and Random Forest}.

\subsubsection{Sequence-based Embeddings:}  

\begin{table}[H]
	\centering
	\caption{Comparison of cross-validation  \gls{MCC} scores for different models using Standard Split and Redundancy Reduction Split based on fold based embeddings for ExoTarget. \gls{MCC}  is a metric that takes place between –1 and +1. +1 means the predictions perfectly match the true classes. 0 means the model is guessing randomly.}
	\resizebox{\textwidth}{!}{ % Ensures table fits within margins
		\begin{tabular}{lcc}
			\toprule
			\textbf{Model} & \textbf{Standard Split MCC} & \textbf{Redundancy Reduction Split MCC} \\
			\midrule
			Random Forest           & 0.58 ± 0.07 & 0.59 ± 0.06 \\
			Logistic Regression     & 0.58 ± 0.04 &  0.58 ± 0.03 \\
			Support Vector Machine  & 0.58 ± 0.05 & 0.57 ± 0.03 \\
			K-Nearest Neighbors     & 0.62 ± 0.06 & 0.62 ± 0.04 \\
			\bottomrule
		\end{tabular}
	}
	
	\label{tab:mcc_comparison_target}
\end{table}



 Table~\ref{tab:mcc_comparison_target} depicts cross-validation  \gls{MCC} scores with their error values among models with different machine learning algorithm with two data-splitting strategies. The cross-validation  \gls{MCC} scores indicates that data splitting methods doesn't significantly make a difference in training performance as it is visible in Random Forest (0.58 ± 0.07 to 0.59 ± 0.06), Logistic Regression (0.58 ± 0.04 to 0.58 ± 0.03), Support Vector Machine (SVM) (0.58 ± 0.05 to 0.57 ± 0.03), K-Nearest Neighbors (KNN) (0.62 ± 0.06 to 0.62 ± 0.04). 
 
\begin{figure}[H]
	\centering\includegraphics[width=0.99\textwidth]{images/learning_curve_target_split.png}
	\caption{\small Learning Curves for Protein Sequence-Based Embeddings}
	\label{fig:sequence_learning_curves}
\end{figure}

Figure~\ref{fig:sequence_learning_curves} displays the  \gls{MCC} cross validation changes across training and validation sets. The analysis of learning can be assessed in two phases: the process of the model learning it with training  \gls{MCC} curve and the level of generalization the model reaches. The training  \gls{MCC} of Logistic Regression, the training  \gls{MCC} conclude around 0.75 at 700 samples and it converges with validation curve which is a sign of improvement in generalization. In contrast, the training  \gls{MCC} remains at 1.00 during 700 training examples in Random Forest and \gls{KNN} models.
In the learning curve plots in Appendix, Figure~\ref{fig:sequence_learning_curves_import} 
which uses the redundancy split training set, it is visible that as the training sample size increases, the overfitting trend decreases among models e.g. \gls{KNN}. Additionally, for both the Logistic Regression model and \gls{SVM}, the training  \gls{MCC} and validation \gls{MCC} values converge at 480 training examples, suggesting improved generalization and model performance.

\subsubsection{Fold-based Embeddings:}  

\begin{table}[H]
	\centering
	\caption{Cross-validation \gls{MCC}  scores for different models using the fold-based embeddings for ExoTarget. The Matthews Correlation Coefficient (MCC) is a metric that takes place between –1 and +1. +1 means the predictions perfectly match the true classes. 0 means the model is guessing randomly.}
	\begin{tabular}{lc}
		\toprule
		\textbf{Model} & \textbf{Standard Split MCC} \\
		\midrule
		Random Forest           & 0.41 ± 0.07 \\
		Logistic Regression     & 0.44 ± 0.05 \\
		Support Vector Machine  & 0.43 ± 0.04 \\
		K-Nearest Neighbors     & 0.40 ± 0.08 \\
		\bottomrule
	\end{tabular}
	
	\label{tab:standard_split_mcc_target}
\end{table}

Table~\ref{tab:standard_split_mcc_target} depicts cross-validation \gls{MCC}  scores with their error values among models with different machine learning algorithm with standard splitting method. The cross-validation \gls{MCC} scores of the classifier indicates that the machine learning models don't significantly make a difference in training performance as it is visible in Random Forest (0.41 ± 0.07), Logistic Regression (0.44 ± 0.05), Support Vector Machine (SVM) (0.43 ± 0.04), K-Nearest Neighbors (KNN) (0.40 ± 0.08). 

\begin{figure}[H]
\centering\includegraphics[width=0.99\textwidth]{images/learning_curve_target_folds.png}
\caption{\small Learning Curve for Protein Fold-Based Embeddings}
\label{fig:fold_learning_curve}
\end{figure}



In Figure~\ref{fig:fold_learning_curve}, displays the \gls{MCC} cross validation changes across training and validation sets. The training \gls{MCC} and validation \gls{MCC}  converge in Logistic Regression and \gls{SVM} which validates the model's ability to generalize unseen data. In contrast, the training \gls{MCC} remains at 1.00 during 600 training examples in Random Forest and \gls{KNN} .

\subsection{Performance Evaluation}

This section evaluates the performance of four machine learning models. Performance assessment relies on the \gls{MCC}. \gls{MCC} bar plots, precision recall curves and confusion matrices interpret the model’s ability to generalize test data.

\subsubsection{MCC Bar Plots}

\begin{figure}[H]
	\centering
	\begin{subfigure}{0.49\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/mcc_target_split.png}
		\caption{}
		\label{fig:mcc_split_target}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.49\textwidth}
		\centering
		\includegraphics[width=\textwidth]{images/mcc_target_folds.png}
		\caption{}
		\label{fig:mcc_fold_target}
	\end{subfigure}
	\caption{\small \gls{MCC} is a metric that takes place between –1 and +1. +1 means the predictions perfectly match the true classes. 0 means the model is guessing randomly.(a) \gls{MCC} Bar Plot for Protein Sequence-Based Embeddings (b) \gls{MCC} Bar Plot for Protein Fold-Based Embeddings.}
	\label{fig:mcc_comparison_target}
\end{figure}

Figure~\ref{fig:mcc_split_target} displays the final \gls{MCC} values for \textbf{sequence-based embeddings} using a \textit{standard split}.
Random Forest and \gls{SVM}  achieve an \gls{MCC} of approximately 0.6, Logistic Regression reaches around 0.5, and \gls{KNN} scores about 0.7 which suggests \gls{KNN} has a better performance than other models. As the test data size increases under a \textit{redundancy reduction split} (see Figure~\ref{fig:mcc_plot_target_import} in the Appendix), Random Forest and KNN improve to an \gls{MCC} of approximately 0.8, while Logistic Regression and SVM maintain a similar \gls{MCC} as before. The performance improvement of \gls{KNN} and Random Forest may be due to the increase in data size.

Figure~\ref{fig:mcc_fold_target} displays the final \gls{MCC} values for \textbf{fold-based embeddings} using a \textit{standard split}. Random Forest, Logistic Regression, and \gls{SVM} each reach an \gls{MCC} of about 0.6, while \gls{KNN} achieves a lower \gls{MCC} of around 0.5. The decrease of \gls{KNN}'s performance when compared to sequence based embeddings may be linked to the limitation of information gathered from fold based embeddings.

\subsubsection{Precision-Recall Curves}

\begin{figure}[H]
\centering\includegraphics[width=0.99\textwidth]{images/precisionrecalltargetsplit.png}
\caption{\small Precision-Recall curve for protein sequence based embeddings. In each plot, the x-axis represents Recall, and the y-axis represents Precision. The black dotted line shows a the chance baseline, which means the points above the diagonal line performs better than random guessing. The blue line depicts the Average Precision (AP). \gls{AP} measures the area under the precision-recall curve. The area under the curve is a metric for model performance.}
\label{fig:pr_curve_sequence}
\end{figure}

Figure~\ref{fig:pr_curve_sequence} shows the precision-recall curves for Random Forest, Logistic Regression, \gls{SVM}, \gls{KNN} - using \textbf{sequence based embeddings} with \textit{standard split}. Random Forest has an \gls{AP} value of 0.96 and \gls{SVM} and Logistic Regression both have a value of 0.95, which shows the models have high performance. On the other hand, \gls{KNN} has an \gls{AP} value of 0.92 with a slight decreasing pattern as recall increases. Plus, all four models performed better than guessing in terms of binary classification based on the chance baseline. As the test data number increases with \textit{redundancy reduction split}, Figure~\ref{fig:precision_recall_target_import} show the \gls{AP} curves among Random Forest, Logistic Regression and \gls{SVM} stays in a similar pattern. 

Figure~\ref{fig:pr_curve_fold}, shows the precision-recall curves for Random Forest, Logistic Regression, \gls{SVM}, \gls{KNN} - using \textbf{fold based embeddings} with \textit{standard split}. \gls{SVM} has an \gls{AP} value of 0.94 and Random Forest and Logistic Regression both have a value of 0.93, which shows the models have high performance. On the other hand, \gls{KNN} has an \gls{AP} value of 0.87 with a slight decreasing pattern as recall increases. Plus, all four models performed better than guessing in terms of binary classification based on the chance baseline. 

\subsubsection{Confusion Matrices}
\begin{figure}[H]
	\centering\includegraphics[width=0.8\textwidth]{images/confusionmatrixtargetsplit.png}
	\caption{\small This confusion matrix displays binary classification protein sequence based embeddings. For each graph, there four cells that represents exotoxin target category: Vertebrates, Non-Vertebrates. Each cell represents percentage of categories that have a certain true label and were predicted to be a certain class. The diagonal cells shows the correct predictions whereas off-diagonal cells (Type I, II errors) are incorrect predictions. Additionally, the cell shading darkens as the percentage values increase.}
	\label{fig:confusion_matrix_sequence}
\end{figure}

Figure~\ref{fig:confusion_matrix_sequence}, shows the confusion matrix for Random Forest, Logistic Regression, \gls{SVM}, \gls{KNN} - using \textbf{sequence based embeddings} with \textit{standard split}. 

For \textit{vertebrates category}, Random Forest 96.10\% is able to identify labels correctly, while 3.90\% mislabeled.
Logistic Regression model predicts 73.38\% correctly while 26.62\% is mislabeled.
\Gls{SVM} model predicts 75.32\% correctly while 24.68\% is mislabeled.
\Gls{KNN}  model predicts 94.16\% correctly while 5.84\% is mislabeled.
The results of the confusion matrices across all models indicate that the correct prediction of the vertebrate category is above average.

For \textit{non-vertebrates category}, Random Forest  52.54\% is able to identify labels correctly, while 47.46\% mislabeled.
Logistic Regression model predicts 81.36\% correctly while 26.62\% is mislabeled.
\Gls{SVM} model predicts 81.36\% correctly while 18.64\% is mislabeled.
\Gls{KNN} model predicts 55.93\% correctly while 44.07\% is mislabeled.
Overall, \gls{SVM} and Logistic Regression demonstrate the highest accuracy in correctly predicting then non-vertebrate category.

In Figure~\ref{fig:confusion_matrix_target_import} in Appendix with \textit{redundancy reduction split}, redundancy reduction split improves the performance of Random Forest and \gls{KNN} since it reduces misclassification percentage among vertebrate classification. This could indicate the possitive effect of increase in data size on model performance.

\begin{figure}[H]
	\centering\includegraphics[width=0.8\textwidth]{images/confusionmatrixtargetfolds.png}
	\caption{\small This confusion matrix displays binary classification protein fold based embeddings. For each graph, there four cells that represents exotoxin target category: Vertebrates, Non-Vertebrates. Each cell represents percentage of categories that have a certain true label and were predicted to be a certain class. The diagonal cells shows the correct predictions whereas off-diagonal cells (Type I, II errors) are incorrect predictions. Additionally, the cell shading darkens as the percentage values increase.}
	\label{fig:confusion_matrix_fold}
\end{figure}

Figure~\ref{fig:confusion_matrix_fold}, shows the confusion matrix for Random Forest, Logistic Regression, \gls{SVM}, \gls{KNN} - using \textbf{fold based embeddings} with \textit{standard split}.

For \textit{vertebrates category}, Random Forest 95.00\% is able to identify labels correctly, while 5.00\% mislabeled.
Logistic Regression model predicts 78.33\% correctly while 21.67\% is mislabeled.
\Gls{SVM} model predicts 80.00\% correctly while 20.00\% is mislabeled.
\Gls{KNN} model predicts 90.00\% correctly while 10.00\% is mislabeled.
The results of the confusion matrices across all models indicate that the correct prediction of the vertebrate category is above average.

For \textit{non-vertebrates category}, Random Forest  54.55\% is able to identify labels correctly, while 45.45\% mislabeled.
Logistic Regression model predicts 83.64\% correctly while 16.36\% is mislabeled.
\Gls{SVM} model predicts 80.00\% correctly while 20.00\% is mislabeled.
\Gls{KNN} model predicts 58.18\% correctly while 41.82\% is mislabeled.
Overall, \gls{SVM} and Logistic Regression demonstrate the highest accuracy in correctly predicting then non-vertebrate category.
